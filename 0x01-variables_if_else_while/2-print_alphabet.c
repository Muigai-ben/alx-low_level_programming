#include <stdio.h>

/**
 * main - alphabet in lowercase,
 * 
 * Return: Always 0 (Success)
 */
int main()
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

